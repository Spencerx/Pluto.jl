import * as semver from "semver"
export default semver

// Note: run `npm install` if you want to get types in your editor

// Note: it was important that this file is called `semver-es` (or whatever) instead of just `semver`. Otherwise `import ... from "semver"` would import the local semver.js file instead of the npm package.